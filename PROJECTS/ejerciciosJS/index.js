//1.Crear n cantidad de Numeros, con valores aleatorios y obtener promedio

/* let numeros = [];

const obtenerNumeros = (cantidad) => {
    for (let i = 1; i <= cantidad; i++) crearNumeroAleatorio();
    console.log("Los numeros creados son =", numeros)
}
const crearNumeroAleatorio = () => numeros.push(Math.floor(Math.random() * 1000));

const sumar = () => numeros.reduce((acc, cur) => acc + cur, 0);
const promediar = () => console.log("El promedio es =", sumar() / numeros.length);

promediar(obtenerNumeros(4)) */